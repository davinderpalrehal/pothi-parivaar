---
topic: Structured author names for Pothi Parivaar books
updated: 2026-09-02T08:19
---

- (event) Express path chosen; slug structured-authors; intake from in-chat request plus live catalog brownfield.
- (direction by user) Books can have multiple authors; keep full name so display format can change later.
- (decision by user) Each author has mandatory first name and last name and optional middle name.
- (decision by user) Current preferred display is first-initial plus last name, e.g. D. Carnegie.
- (capability) CAP-1 Capture structured name parts per author so first and last are required and middle is optional.
- (capability) CAP-2 Attach one or more authors to a book with order preserved.
- (capability) CAP-3 Derive catalog display from stored name parts without discarding the full name.
- (capability) CAP-4 Search and locate still match a book by any stored author name part or the derived short form.
- (capability) CAP-5 ISBN lookup prefills structured authors that stay editable before save.
- (constraint) First and last name are required for every author; middle is optional.
- (constraint) Display short form is derived from name parts and is not the only stored copy of the name.
- (constraint) A book must have at least one author.
- (constraint) Author order on the book is the display and save order.
- (constraint) Catalog cards, detail, add/edit, Hermes locate/recommend, and keyword search must keep working against the new author model.
- (assumption) Current derived form ignores middle name: Dale Carnegie -> D. Carnegie, not D. M. Carnegie.
- (assumption) Multiple authors display as comma-separated short forms in listed order.
- (assumption) Authors are recorded on the book; no shared author identity or merge-across-titles catalog.
- (assumption) Create and edit still require title plus at least one valid author, replacing the single author string.
- (question) How should ISBN/Open Library full-name strings be split into first, middle, last before the user edits them?
- (question) What happens to existing books whose author is already a short form or a comma-joined string?
- (question) How are mononyms or authors with no first/last split (Plato, Madonna) saved if first and last are both required?
- (note) Wrapper-only dropped: BMad help routing and skill menu codes. PRD FR-1 already says Author(s); implementation stored one author string.
- (event) Self-validate pass 1 coherence: PASS. CAP-1..5 each have intent+success; intents are WHAT; constraints bend design; non-goals present; success signal demonstrable; IDs stable; prose lean.
- (event) Self-validate pass 2 preservation: PASS. User claims (multi-author, D. Carnegie practice, keep full name, first/last required, middle optional) landed in kernel or brownfield.md. FR-1/FR-2/FR-6/Hermes author surfaces preserved. ISBN split, existing-row migration, and mononyms flagged as open questions. Wrapper-only: BMad help routing.
- (decision by user) Authors are their own table so the family can search by author name in the future.
- (decision by user) Authors are not required on a book; magazines and similar titles may have a publishing house instead of an author.
- (decision by user) ISBN/Open Library names: split on comma for authors; each name split on space; first token is first name, last token is last name, leftover tokens joined with space are middle name.
- (decision by user) Existing books get a one-time script that applies the same name-split rules.
- (decision by user) Mononyms store the name as first name and last name as a single space.
- (capability) CAP-1 updated: last name may be a single space for mononyms; first name still required; middle optional.
- (capability) CAP-2 updated: a book may have zero or more authors; order preserved when more than one.
- (capability) CAP-5 updated: ISBN prefill uses the comma/space split in name-rules.md.
- (capability) CAP-6 Authors are first-class records so a name can be found and reused across books.
- (capability) CAP-7 Existing Book.author strings are converted once by the same split rules.
- (constraint) Supersedes book-must-have-an-author: a book may have zero authors.
- (constraint) Supersedes authors-on-the-book-only: author is its own record; books link to authors.
- (constraint) Last name may be a single space; that is the mononym form.
- (constraint) Name split and display rules in name-rules.md are the contract for ISBN, the migration script, and catalog short form.
- (assumption) Same first+middle+last reuses one author record so later author search finds every linked book.
- (assumption) Empty or missing author string on an existing book yields zero authors; the split is not invented.
- (assumption) Create/edit require title only; authors are optional.
- (question) Should publisher/publishing house be a first-class field for titles with no author?
- (question) How should the derived short form render when last name is a space (mononym)?
- (event) Self-validate pass 1 coherence: PASS. CAP-1..7 have intent+success; CAP IDs preserved and extended; superseded required-author and on-book-only constraints overridden by later entries; name-rules.md holds split/display; prose lean.
- (event) Self-validate pass 2 preservation: PASS. New user claims (author table, optional authors, comma/space split, one-time script, mononym last=space) landed in kernel or name-rules.md. Publisher field and mononym short-form render left as open questions. Wrapper-only: none this update.
- (decision by user) Publisher is a first-class record with its own table; attaching a publisher to a book is optional at this stage.
- (decision by user) Mononym catalog display is the first name as-is: Plato, Madonna, Cher — not P. or M. or C.
- (capability) CAP-3 updated: mononym short form is the first name unchanged when last name is a space.
- (capability) CAP-8 Publishers exist as their own records; a book may optionally link to a publisher.
- (constraint) Publisher is its own record; a book is not required to have one.
- (constraint) When last name is a single space, catalog display is the first name only.
- (assumption) A book links to at most one publisher at this stage.
- (assumption) A publisher is identified by a single organization name, not person name parts; same name reuses one record.
- (assumption) ISBN prefill and the author migration do not create or attach publishers.
- (event) Self-validate pass 1 coherence: PASS. CAP-8 added; CAP-3 success now includes mononym display; open questions section emptied; publishers.md holds cardinality and name shape.
- (event) Self-validate pass 2 preservation: PASS. Publisher-as-own-optional-table and mononym display Plato/Madonna/Cher landed. No remaining open questions from this intake.
- (decision by user) Story breakdown requested; stories are highest sprint priority so a new session picks them first.
- (decision by user) Story 1 Author publisher persistence: schema, API, migration, search/Hermes; no form changes. spec_checkpoint=false done_checkpoint=false.
- (decision by user) Story 2 Author add edit UI: structured author fields and optional publisher. spec_checkpoint=false done_checkpoint=false.
- (decision by user) Story 3 ISBN author name split: name-rules prefill only. spec_checkpoint=false done_checkpoint=false.
- (event) stories.yaml validity: three entries, quoted ids 1-2-3, unique, prefix-free, no status field, list order is execution order.
- (decision by user) Keep titles inside first/middle/last (no title columns); may reverse after testing.
- (direction by user) Prefer the approach that migrates the fewest existing author records.
- (decision) Least-migration path: no new columns, no second split/rewrite of stored authors; change only derived short form (and later ISBN prefill for new strings).
- (decision) Short form is derived from the reconstructed token sequence (first + middle tokens + last), not first-char-of-first-name + last-name field. Known prefix titles and suffix honorifics are recognized on that sequence so last=ji still yields Singh as the family name.
- (assumption) Existing badly split rows stay as stored; display-time recognition fixes cards without rewriting rows. Identity remains same first+middle+last.
- (constraint) Do not add title_prefix/title_suffix columns in this update. Do not re-run CAP-7 author-string migration for honorifics.
- (question) Which prefix titles and suffix honorifics are in the known list, and what is each short abbreviation?
- (direction by user) Seed as many recognized titles as possible; missed ones will be added in daily use.
- (decision by user) Recognized titles must be addable by an admin/household editor without a code change or deploy.
- (capability) CAP-9 A household member can add, edit, disable, or remove recognized author honorifics (prefix or suffix, match tokens, card abbreviation) that the short-form deriver uses immediately.
- (capability) CAP-3 updated: derived short form peels recognized prefixes/suffixes from the reconstructed token sequence, then applies initial+last (or mononym), then prepends the title abbreviation.
- (capability) CAP-4 updated: search also matches recognized title tokens and their abbreviations (Dr., BHB).
- (constraint) Honorific recognition is data: a persisted editable list seeded from honorifics.md. Code ships a deriver and an editor, not a closed title enum.
- (constraint) Match longest token sequence first, case-insensitive, optional trailing period on a token. Prefixes peel from the front; suffixes from the end. Remaining tokens are the personal name.
- (constraint) Singh, Kaur, and other given/family name tokens are never seeded as titles. Guru is omitted from the seed (too often a name).
- (constraint) No login/role gate for the honorific editor in this work; any household user of the app can maintain the list.
- (constraint) Empty abbreviation means omit that honorific from the card (used for ji). Multiple peeled prefixes emit abbreviations in peel order, space-separated.
- (assumption) ISBN/migration split rules stay comma-then-space; they do not rewrite stored authors when the honorific list changes. Only derived short form (and search against it) changes.
- (assumption) Gyani and Giani are two seed spellings of one abbreviation Gyani. Dr/Dr./Doctor share Dr. Prof/Prof./Professor share Prof.
- (assumption) Bhai alone stays Bhai on the card (not B.). Sardar shortens to S. Sardarni to Sdn. Bhai Sahib Bhai to BHB as the user specified.
- (assumption) Bibi Sahib Bibi shortens to BSB by parallel with BHB. Singh Sahib to S.S. Bhai Sahib to B.S.
- (note) stories.yaml story 2/3 descriptions predate honorifics and CAP-9; update itself does not rewrite stories.yaml.
- (event) Self-validate pass 1 coherence: PASS. CAP-1..9 intent+success; CAP-3/4 updated in place; CAP-9 new; IDs stable; constraints bend storage/display/editor; non-goals include no second migration and no Singh/Kaur/Guru seed; success signal demonstrable.
- (event) Self-validate pass 2 preservation: PASS. User claims (keep titles in name parts, least migration, Dr. D. Singh, BHB V. Singh, large seed list, admin add without code change) landed in SPEC.md, name-rules.md, honorifics.md. Wrapper-only: BMad help/spec routing.
